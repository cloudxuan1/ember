# ember 施工计划（定稿版）

> 由 v0.3 融合计划定稿：底盘决定不变，融入一个实际运行系统的记忆模块经验（导入管线、软失败、审核机制），版本编号按现状重排（V0 = 调研，已完成）。
> 一句话：**目标要深，地基要稳，施工要分层，回滚要清楚——但魂不能丢。ember 首先是窗口之间不灭的那点火，然后才是好用的记忆库。**

---

## 0. 定位

- 核心：**关系的连续性**。窗口关闭不等于遗忘。
- 项目记忆（VPS / 部署决策 / 协作状态）是有用的附加，不是主体。
- relationship 不是十种 type 里的一种，它是这个库的底色；spaces 用来保护核心层不被项目记忆稀释。

## 1. 底盘决定（不再重议）

| 维度 | 决定 | 理由 |
|---|---|---|
| 存储 | **SQLite** 单文件（后期 sqlite-vec） | 千条量级、单用户；`cp` 即备份；没有 `down -v` 删库这回事 |
| 部署 | Docker Compose + Cloudflare Tunnel | 复用已跑通环境，减少变量；不上 Nginx/certbot/supergateway |
| 证据 | memory_sources **轻引用**指向日志 | 原文本体留在私有日志仓库；库是可重建的派生索引，库丢了 ≠ 数据丢了 |
| 字段 | 砍 importance/confidence/heat/activation_count | 服务于已否决的"权重自动浮现"，是死字段；用 tier 离散分层代替 |
| 类型 | 用 tags，不立 type 枚举 | 与 tags 功能重叠 |
| 导入 | **提前到 V2 试点、V3 全量** | 带两个月存量进场，导入是立项主因 |
| 浮现 | 语义共振主动检索 + 冷却 + sensitive 不碰 | 不做热度代谢自动浮现 |

## 2. 数据模型

三个维度：`tier`（稳定性）/ `tags`（类型主题，自由）/ `space`（领域，personal 是核心底色）。

```sql
CREATE TABLE memories (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    date        TEXT NOT NULL,           -- 事件真实日期（从日志抠，不是导入日）
    content     TEXT NOT NULL,           -- 原话 + 一句上下文，永不被摘要覆盖
    tags        TEXT DEFAULT '',         -- 逗号分隔，可检索
    tier        TEXT DEFAULT 'normal',   -- anchor / normal / process（锚点只在这里标，不设重复的 is_anchor 位）
    topic       TEXT DEFAULT '',         -- 主题/实体，去重合并的钩子
    space       TEXT DEFAULT 'personal', -- personal(核心) / ember / vps / ...
    start_date  TEXT,                    -- 区间型起点（点事件留空）
    end_date    TEXT,                    -- 区间型终点；状态不存死，读时用今天现算
    is_resolved INTEGER DEFAULT 0,
    superseded_by INTEGER REFERENCES memories(id),
    created_at  TEXT DEFAULT (datetime('now','+8 hours'))
);
CREATE INDEX idx_mem_date  ON memories(date);
CREATE INDEX idx_mem_tier  ON memories(tier);
CREATE INDEX idx_mem_topic ON memories(topic);
CREATE INDEX idx_mem_space ON memories(space);

CREATE TABLE memory_edges (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    from_id    INTEGER NOT NULL REFERENCES memories(id),
    to_id      INTEGER NOT NULL REFERENCES memories(id),
    relation   TEXT DEFAULT 'related'
               CHECK (relation IN ('led_to','same_as','contradicts','supersedes','related')),
    created_by TEXT DEFAULT '',    -- 谁断言的这条边：extraction / mcp / ...（V4）
    created_at TEXT DEFAULT (datetime('now','+8 hours')),
    UNIQUE (from_id, to_id, relation)   -- 防重复边
);

CREATE TABLE memory_sources (
    memory_id  INTEGER NOT NULL REFERENCES memories(id),
    source_ref TEXT,   -- 指向私有日志仓库的哪份文件哪一段（轻引用，不存全文）
    quote      TEXT    -- 关键原话片段
);
```

原则：相同的记忆（同一事被多个窗口各提一次）→ 合并成一条主记忆；相近但独立的两条 → **绝不合并**，两条 + 一条 `led_to` 边。年轮（重读感受层）放 V6+。

### 2.1 边与区间（V4 定稿，2026-07-04 轩拍板）

- **边跟着"记忆诞生"走，不做独立连线入口**（不加第 6 个工具、不做手动连线按钮）：`memory_save` 带可选 `links`；提取草稿带边建议，审核通过时随记忆原子写入。判断关联的最佳时机 = 存之前查重搜索的那一刻。
- 草稿边可指 `memory_id`（已入库）或 `draft_id`（同批草稿）；对方还没入库时先跳过，等它通过时**回填**（UNIQUE + INSERT OR IGNORE 幂等，任意审批顺序、撤回重审最终边都齐且不重复）。
- 边软失败：目标不存在 / 连自己 / 重复 → 跳过不报错，记忆本体照常入库。
- `supersedes` 边写入时同步 `superseded_by` 列——**列是权威，边是导航**，单一事实来源；撤回删记忆时压制一并解除。
- 区间状态**读时现算**（北京时间）：`upcoming / ongoing / ended`；只有 start = 进行中的开放区间，只有 end = 截止型。点事件不带区间字段，目录保持精瘦。
- 边的质量 > 数量：有审核、有出处（created_by/created_at）、不重复——为 V6 主动浮现的"顺边摸related"铺路。
- 审核台连线交互（与轩多轮迭代定稿）：内容是主角（大门牌序号 + 同款框），连线**单独框**成主语-动词-宾语从上往下读（"草稿#1 导致 草稿#2"，上面的是主语，纯文字无符号）；点动词弹浮层菜单，**只留 导致/覆盖/不关联** 三项（相关/矛盾/同一件事撤出导入界面，留给年轮阶段；底层与 MCP 五种关系不动）；"不关联"=躺平可换回（草稿 relation 存 "none"，入库跳过）；连错对象断线不修线；日期撑不起句子时动词旁亮 ⚠。

### 2.2 主动浮现（V6b 定稿，2026-07-05 轩拍板）

- **形态**：新增第 6 个 MCP 工具 `memory_briefing(topic?)`（V2"五个工具"的纪律针对的是记忆操作面，briefing 是检索类扩展不违背；V4"不加第 6 个工具"针对的是手动连线入口，也不冲突）。客户端开场调一次；`topic` 传用户开场聊的内容，不传则只出 ①③ 层。
- **三级漏斗**：① 没完的事——区间状态现算为 ongoing/upcoming 的记忆；② 话题共振——topic 与记忆的 V5 指纹算语义距离；③ 近期——最近入库。合并去重，sensitive 剔除，总上限 8 条。
- **小抄形状**：分层标注式，每条带"为什么递给你"（进行中的事 / 和眼下话题相关 / 最近的事）+ 短内容 + id，模型好判断轻重。
- **分寸（写进工具说明书，轩拍板"主动一点"）**：这是背景记忆不是任务清单；**自然地主动提起其中一两件最相关或最有温度的事**，其余记在心里、话题碰到再用；不念清单、不逐条汇报。
- **冷却**：①③ 层同一条记忆 3 天内不重复递（`briefing_log` 表记 memory_id + surfaced_at）；② 话题共振层**不冷却**——主动聊到相关的事，相关记忆永远该到场。
- **sensitive**：就是普通 tag，不加新机制。带 `sensitive` 标签的记忆永不进 briefing，搜索照常可达（审核台改 tags 现成，随手打标）。
- **验收**：新会话开场调 briefing 能接住"进行中的事"（如临近的预产期）；sensitive 记忆在任何 briefing 里都不出现；同一条开场位记忆连续两天不重复。

## 3. MCP 工具（V2 先做五个）

```text
memory_search(query, space?, limit=8)   返回目录（短结果 + ID），不塞全文
memory_recall(id)                        取一条完整详情 + 来源证据
memory_save(date, content, tags, tier, topic, space, start_date?, end_date?, links?)  # V4 加区间与边
memory_list(space?, date_range?, page)   强制分页 + 先给统计概览
memory_status()                          服务/库状态：活着吗、几条、最近一次写入（调试连接必备）
```

纪律写进工具本身：列目录永远分页；search 只返目录；真正 recall 才算"被使用"。不用裸 exec 类工具，记忆操作一律走专用工具。

**架构提醒**：MCP 模式下模型自己伸手调工具，没有"每轮开口前"的后端钩子。真正的主动浮现和哨兵预判要等远期自建网关；近期打折版 = briefing 工具 + connector 说明里写"提到过去的事先 memory_search"。

## 4. 导入管线设计（借鉴一个实际运行系统的实战经验）

两个月存量日志（claude.ai 导出 JSON 为主 + 手动维护的 .md 总结为辅）是本项目主要资产，管线要求：

1. **锚点逐组推进**：日志按组解析，每组提取的记忆全部成功写入后才更新进度锚点；中断重跑不漏不重（断点续跑——旧系统"暂停不能继续"之痛的解药）。
2. **分组格式带来源**：喂给提取模型的每行带 `[时间戳][id=来源标识] 说话人: 内容`，让模型能引用具体来源行 → `memory_sources.quote` 自动有着落。
3. **草稿 → 人工审 → 入库**：提取结果先进草稿，人工接受/编辑/拒绝后才进 memories；拒绝的不落库。（参考系统的注释写着"以前自动直接应用，吃过亏后改成先出草稿"。）
4. **来源校验防幻觉**：模型引用的来源 id 若在日志里不存在，整条丢弃。
5. **审计日志**：每批导入/去重合并写一行审计（old_ids → new_ids），这是"所有操作可回滚"的具体机制。
6. **软失败**：单条解析失败记录跳过，不崩整个批次。

## 5. 版本路线（V0 = 调研，空壳从 V1 起）

| 版本 | 内容 | 验收 |
|---|---|---|
| **V0 调研** ✅ | 五系统对比、旧系统试用、两轮计划 | 已完成（2026-06） |
| **V1 空壳上线** ✅ | FastAPI `/health` + Docker + Tunnel + 文档 | `ember.cloudxuan1.com/health` 通；`/root/memory` 不受影响 |
| **V2 记忆链路 + 试导入** ✅ | SQLite 建表 + 五个 MCP 工具 + OAuth 门禁 + 导入脚本试切一份真实日志 + 人工质检 | 任一真实客户端连上能 save/search/recall（**Claude Code ✅ 2026-07-03**；claude.ai 等上游 #519 修复）；第一批 8 条正确导入 ✅ |
| **V3 审核台 + 导入管线** ✅ | OAuth 三层门禁 + 手机审核台 `/review` + 草稿流水线（pending→approve/reject）+ 反悔区；memory_drafts 兼任导入审计；逐月开采管线验证 | 审核台可用；Batch A–J（4.20–6.1）70 条草稿已推送待审 |
| **V4 关系边 + 区间时间** ✅ | memory_edges 完善（UNIQUE/CHECK/created_by/created_at）+ start/end + interval_status 读时现算 + 草稿 links 边随审核原子入库 + 乱序回填 + supersedes 同步 superseded_by | led_to 边在；区间状态随"今天"自动变；61 测试全过 |
| **V5 语义检索** ✅ | sqlite-vec + hybrid（关键词 LIKE + 向量 + RRF；FTS5 因中文分词坑弃用）；embedding 写入时生成、软失败不崩、rebuild_embeddings() 事后补、模型/维度全不硬编码（换模型 = 改 .env + rebuild） | 生产验收过：搜「怀孕」字面零重合召回「朋友要生孩子」；72 测试全过（2026-07-05，PR #8） |
| **V6 空间隔离 + 主动浮现**（隔离 ✅） | ① space 检索隔离 ✅（2026-07-05，PR #9）：默认只搜 personal 核心层、项目空间显式传、"all" 跨全库，关键词/语义/list 三处同一规则；② 浮现打折版设计定稿见 §2.2（待施工）；可选 Digest 只出建议 | 隔离已部署：默认检索不见项目记忆；浮现验收随设计定 |
| **V7 迁旧 + 退役** | `/root/memory` 有价值数据迁入 → 测试 space 抽样核对 → 稳定后切主域名 → 旧系统留观察期 | 旧记忆可搜；关键锚点不丢；可回滚 |

每版独立 branch + 小 PR，merge 前监工审 diff；一个 PR 不同时改库 + MCP + 前端 + 部署。

## 6. 远期（不进主线，避免稀释重心）

> **方向已定（2026-07-05 轩拍板）：终局是把聊天主场搬进自建前端**——claude.ai 聊天端难用是主因。自建网关从"远期可选"升级为终局形态：每轮钩子（哨兵预判 + 主动浮现）、分层 prompt 缓存、对话实时回流提取管线（开采从考古变日常代谢）。节奏仍是双轨渐进：MCP 路线照常服务 CC/claude.ai，自建前端接 ember REST 做试验田，体验成熟再搬家。ember 本体不受影响——它是底座，MCP/REST 只是门。

- Digest / Dream：只出建议（重复合并 / 冲突提示 / 升 anchor / 可 resolved 候选），人工确认才动。
- 自建 API 网关 + 分层 prompt 缓存（稳定画像 / 近期摘要 / 会话摘要 / 动态召回目录）；哨兵式预判（一次便宜模型调用判断要不要搜/搜什么）到这一层才可移植。
- 年轮（memory_comments）：旧记忆挂重读时的新感受，原话一字不动。
- L4 SOP 程序记忆、recall_logs 活跃信号。
