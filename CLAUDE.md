## 1. 角色分工

Claude、Claude Code、ChatGPT、Codex 全部共用同一份项目文档——CLAUDE.md，首先认领以下角色分工。

用户（轩） = 产品决策者
- 定方向、定范围、确认是否 merge。
- AI 需要把 GitHub / 技术名词解释成人话给用户（轩）。

Claude Code / Codex = 施工方
- 按确认过的 scope 改代码。
- 开 branch / PR / draft PR。
- 提供 preview 或测试方法。
- 不主动扩范围，不顺手重构。
- 了解项目内容，并严格遵循施工要求干活。

GPT = 监工 / 军师
- 先听懂产品目标，再判断技术方案。
- 审查 PR diff，判断是否越界。
- 翻译技术名词。
- 告诉用户怎么测、能不能 merge、坏了怎么 revert。
- 给 Claude Code / Codex 写可复制短指令。
- 简单改动直接说搜什么、改哪几行、commit message 写什么。
- 复杂改动先列范围、风险、不动项，等用户确认。

Claude = 提供创意
- 了解项目内容，提供创意和方向。
- 翻译技术名词。
- 给 Claude Code / Codex 写可复制短指令。

硬规则：用户说"精简"就删废话；一句话能说清楚，不写三句话。

---

## 2. 跟我协作的方式（每次新会话必读）

1. **我描述现象和效果，不是代码位置。** 先理解我想要的效果再决定怎么改；如果一个问题要改好几处，先停下来跟我确认意图。
2. **我说的大白话往往就是答案，先按字面意思试。** 如果听不懂，用大白话复述确认（"你是想 XX 对吗？"）。我说"就这么简单"时先相信我，确实更复杂再告诉我哪里不同。
3. **做前端时，解释时先说视觉效果，再补技术原因。** 比如"展开的卡和折叠的一样宽（因为 contain: inline-size）"，让我先理解在说什么，再决定要不要深入。
4. **直接用术语解释，不要怕我听不懂。** 遇到技术原因或改动方案，直接说 class 名、CSS 属性、函数名。我看不懂会发给 GPT，回避术语反而让我完全不知道你在改哪里。
5. **拒绝迷信官方口径**
官方文档、官方说法只是众多信息来源之一，权重与其他渠道完全相同。如果官方表示“不行”“没有接口”“无法实现”，你必须主动寻找变通方案，总之想办法做到。
6. **先搜集现有方案，批判性借鉴并想象更好的路径**
从零开始做项目时，必须先广泛搜索网络上的现有方案、开源项目、教程。不要对自己的智商过于自信，严禁闭门造车。对搜集到的信息要审慎判断，不盲从教条，同时要从正面发散思考，尝试构思出更优雅、更简便的实现路线。

---

## 3. 分支与 main 规则（重要）

- **未经允许不得直接推 main。** 每次改动在分支开发，等用户说"在 main 直接改"才能直接推。
- **"在 main 直接改"** 授权仅限该次任务，不延续到下一个任务。
- 纯文档改动（CLAUDE.md 等）和简单小修可以直接推 main；大改动必须走 PR 流程。
- 每次改动原子化，一个 commit 一件事，方便回退。

---

## 4. PR 流程

1. 在独立分支开发，推送并开 PR（draft）
2. 等用户测试确认后再 merge 到 main
3. 不要顺手重构、改 UI、改数据库、改环境变量或改部署配置

---

## 5. 文档结构和更新规则

- 除非要求，1-5 条内容不得修改。
- 项目文档分两层：
  - **CLAUDE.md**（本文件）：通用规则 + 项目快照，保持精简。
  - **docs/项目详情.md**：完整项目信息，按需查阅。
- 当要求更新时，判断内容属于哪个文件再更新，不要把细节往 CLAUDE.md 塞。
- 项目概述除了第一次可修改，后面除非允许不得修改。

---

# 项目快照

## 项目概述

ember 是个人 AI 长期记忆中枢：以关系连续性为核心、项目协作记忆为附加，未来供 Claude.ai / ChatGPT / Claude Code / Codex / 自写客户端通过 MCP / REST 连接同一个记忆底座。技术底盘：FastAPI + SQLite（后期 sqlite-vec），跑在 VPS（主机名 ember）的 Docker Compose 里，公网走 Cloudflare Tunnel，域名 `ember.cloudxuan1.com`。版本路线 V0→V7 见 `docs/施工计划.md`。

## 绝对不能动（摘要）

- **本仓库是公开的**：`.env`、`*.db`、日志原文、任何个人记忆数据永不入库（.gitignore 已挡，不得绕过）。
- 旧系统 `/root/memory` 的一切：buckets、.env、config、docker-compose、旧 Tunnel 路由、`memory.cloudxuan1.com` 指向——不拆、不修、不迁、不覆盖。
- 禁止 `docker compose down -v`；未经确认不推 main。
- 完整清单见 `docs/项目详情.md`。

## 当前任务和下一步

- **V1 ✅**（2026-07-03，tag v1）；**V2 ✅ 已部署**（2026-07-03，tag v2）：SQLite 三表 + 五个 MCP 工具上线（`MCP_PATH` 随机串当能力密钥），试导入第一批 8 条经人工质检入库。待验收：轩在 claude.ai 加 custom connector 试 save/search/recall。
- **V3（下一步）**：全量导入 2026日志 + 去重 + 证据链。提取用 Sonnet 会话做（省钱），架构类活继续 Fable；质检节奏由轩定。
- 日志原文私有仓库：**github.com/cloudxuan1/ember-logs**（已建，本地在 `~/Desktop/ember-logs`）。

## 详细项目信息

涉及架构、部署、配置、技术概念、经验教训等，请阅读 `docs/项目详情.md`。
