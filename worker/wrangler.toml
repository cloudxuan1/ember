# Cloudflare Worker 配置
# 部署（在 worker/ 目录下执行）：  npx wrangler deploy
# 设置密钥：                       npx wrangler secret put OPENROUTER_API_KEY
name = "ember-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"
