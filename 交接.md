# ember 交接

> **本文件顶部永远是最新状态，往下按时间倒序，再往下是技术参考。**
> 接手先读「仪表盘」（2 分钟看完现状），要查踩坑史/决策往下翻「历史日志」，要写代码/部署/排障查最下面「技术参考」。
> 规矩：**每次收工前更新仪表盘**；过时信息直接删掉重写，不留存档（这条只指仪表盘——历史日志照常倒序保留，不删）。
> 处理私人聊天日志的操作规程（怎么读日志、怎么切 batch、认证 token）不在本文件——那部分留在私有仓库 `ember-logs`。

---

# 仪表盘

**最后更新：2026-09-16（Codex）** ｜ **当前版本：记忆库 V0–V6 生产运行；网页 connector 已复验；聊天主场已改为独立 chat-lite；安全自动部署已上线并完成首次验收**

**09-16 增量**：聊天主场改为独立 `chat-lite`，ember 只做记忆后端；旧 `feat/chat-gateway` 留作以后写入流程的参考，不继续部署。PR #15 已合并并由 Codex 安装到 VPS：5 分钟轻量轮询，无更新静默退出，纯文档只同步，后端或容器配置变化才做 SQLite 一致性快照、重建和健康检查，失败自动回退程序但不自动覆盖数据库。安装前后容器未重启，`/health` 正常。

**09-15 运维增量**：生产环境认证材料完成例行轮换；`/health`、`/review`、OAuth 发现端点和网页登录均验收通过，旧 MCP 路径已失效；claude.ai 网页 connector 重新连接成功，`memory_status` 只读调用通过。数据库和记忆内容未改动，未发生数据迁移；密钥值仍只存于 VPS `.env`，不进仓库。

## 新窗口接手四步

1. 读完本仪表盘（现状快照 + 待办队列 + 常用命令），约 2 分钟。
2. 协作规矩在 `CLAUDE.md`（角色分工 + 协作方式 + 分支/PR 规则），必读一遍（CC 读到项目内文件时会自动加载它）。
3. **漏更校验**：对比上方"最后更新"日期和 `git log -1 --format=%cd --date=short`——若 git 比文档新，说明上个窗口没收工，先按 git log 补交接再干活。
4. 待办队列最上面那条 = 下一件事。查历史决策/踩坑：本文件往下翻「历史日志」；查技术细节：往下翻「技术参考」；查版本路线/设计理由：`docs/项目计划.md`。

## 现状快照

- **记忆库主链路（V0–V6）全部在生产**：存/搜/看/隔离/主动浮现全链路可用；真库 79 条记忆 + 另有待审草稿（数量见私有仓库 ember-logs 的操作记录）。空间隔离已上线（检索默认只搜 personal 核心层，项目话题显式传空间名，`"all"` 跨全库）。
- **审核台三连 PR 已合并部署**（2026-07-12，#12→#13→#14）：「＋ 添加」手动补记忆、配色翻新（奶油亮色 #FFF6DE/#8BDFDD/#F48F68/#FFE394）、字体升级（自托管 Noto Serif SC / Noto Sans SC / Fira Code）。设计定稿沉淀进 `~/.claude/skills/xuan-design/`。
- **claude.ai 网页 connector 已接通并重新验收**（2026-09-15）：认证轮换后重新连接，`memory_status` 只读调用通过。首次接通时的根因是 Cloudflare「Block AI training bots」拦截正式 MCP 请求，完整排障过程见「技术参考 → 经验教训」。
- **已知空转点**：全库暂无区间型记忆（`start_date`/`end_date`），briefing 工具的「没完的事」这一层暂时没原料——以后提取时把进行中/有期限的事填上区间字段就有了。
- **聊天前端 = 独立 chat-lite 仓库**：ember 不再自建聊天页。第一步由 ember 提供独立只读聚合接口，chat-lite 的 Worker 在服务器侧调用；以后再增加独立的草稿写入凭据，写入只进待审草稿，不直达正式记忆。`feat/chat-gateway` 分支未部署，保留作写入流程参考。
- **安全自动部署已上线**（PR #15，`081c01e`）：5 分钟检查一次 `origin/main`；没更新不重建、不碰数据库。只有 `backend/**` 或 `docker-compose.yml` 变化才拍在线一致性快照并重建；失败回退程序，数据库快照留给人工判断。安装时已确认 cron、当前健康提交和容器不重启。

## 待办队列（最上面 = 下一件事）

1. **轩与小克审 chat-lite 接入讨论稿**：见 `docs/产出/260916-chat-lite接入ember讨论稿.md`。先收批注，不动代码；讨论定稿后再拆 Ember 只读接口 PR。
2. **chat-lite Worker 接只读记忆**：新会话首轮取 briefing，每轮按问题 search；动态记忆跟随当前用户消息，不改稳定 system prompt；Ember 超时则正常聊天。
3. **只读稳定后再做写入**：另设 `EMBER_DRAFT_TOKEN` 和草稿接口；chat-lite 只能创建 pending 草稿，仍由轩在审核台通过后才进正式记忆。
4. 主动浮现和现有记忆库 bug 随用随报，一条一个 PR；`docs/产出/ember人话说明书.md` 等 chat-lite 接通后再补使用章节。

## 常用命令

```bash
# 自动部署已安装；查看任务、事件日志，或人工重试当前 main
crontab -l | grep autodeploy
tail -n 50 /root/ember/data/autodeploy.log
FORCE=1 /root/ember/scripts/autodeploy.sh
# 自动部署未安装或暂停时的手动后备
cd /root/ember && git pull --ff-only && docker compose up -d --build

# 换 embedding 模型
# 1. 改 VPS .env 的 EMBEDDING_MODEL
# 2. 重启容器
# 3. 补算旧记忆指纹（旧指纹自动失效，重算前搜索安全降级成纯关键词）
ssh ember 'docker exec ember python -m app.embeddings'

# 查密钥位置（MCP_PATH / OAuth / embedding key，不要把值抄进任何文档）
ssh ember 'grep -E "MCP_PATH|EMBER_OAUTH|EMBER_REVIEW|EMBEDDING" /root/ember/.env'

# 在线一致性备份（自动部署会自行执行；手动重大改动前可单独跑）
python3 /root/ember/scripts/sqlite_backup.py /root/ember/data/ember.db /root/ember/data/ember.db.pre-<版本>.bak
```

## 协作方式速记

分工：Claude 负责云端讨论、分支和 PR；Codex 负责本地仓库、测试、VPS、部署和 Secrets；轩确认方案并决定是否 merge。完整规矩见 `CLAUDE.md`。

---

# 历史日志

> 最新在最上，往下越来越旧。保留决策/教训/PR 号，过程细节已压缩。

## 2026-09-16 · chat-lite 路线收口 + 自动部署 PR #15 安全修订

- 聊天主场确定为独立 `chat-lite`；ember 只提供记忆能力。接入顺序定为独立只读接口 → chat-lite Worker 只读接入 → 稳定后增加独立草稿写入接口，正式记忆始终保留人工审核闸。
- 新增 `docs/产出/260916-chat-lite接入ember讨论稿.md`，记录两仓实际结构、缓存与 briefing 冷却约束、读写分权、预览隔离、分阶段施工顺序和待小克审查的问题；当前仅讨论，未改应用代码。
- PR #15 从 2 分钟全量重建改为 5 分钟轻量轮询：无提交静默退出，纯文档只快进；运行代码变化才用 SQLite Backup API 拍一致性快照、重建并检查 `/health`。失败只自动回退程序，数据库不自动覆盖；加入并发锁、坏提交暂停重试和日志轮换。
- 安装方式改为执行仓库内已审核脚本，不再使用网络脚本直接管道执行。后端 104 项测试和 GitHub Actions 均通过；PR #15 已合并为 `081c01e`，VPS cron 已安装。安装前后容器 ID 与启动时间一致，证明安装没有重建；`/health` 正常。

## 2026-09-15 · 生产认证轮换 + connector 复验

- 轮换生产 `MCP_PATH` 及 OAuth 认证材料，密钥未输出、未写入文档或仓库；VPS 保留权限受限的回滚备份，审核台脚本鉴权未受影响。
- 生产容器重建后，内网健康检查与登录验证通过；公网 `/health`、`/review` 和 OAuth 发现端点均返回 200，旧 MCP 路径返回 404，新路径未授权请求返回 401。
- claude.ai 网页 connector 完成 OAuth 授权并连接成功，`memory_status` 只读实调通过。本次只改认证层，未修改代码、数据库或记忆内容。

## 2026-07-21 · 前端完整方案出稿 + 阶段0详案 + 可点示意稿

- 轩逐条拍板:单仓库(前端住 `frontend/`)/浅 PWA(添加到主屏幕即可,不做离线缓存)/旧审核台原位保留+设置页跳转(域名走路径式,`/`=新前端,`/review` 不动)/大脑先接 API、CC 留二期/群聊=轩发言每 AI 一轮+@点名单独答(浅抄 Aion,不给第二 AI 做记忆)/中栏首发日记+纪念日历/单聊全家桶(thinking/联网/图片/缓存徽章/上下文压缩)/分阶段分会话施工。
- 三处调查落地进方案:**homunculus**(MIT,CC/API 双模路由完整样板,二期抄它)、**anno-mcp**(共读交互好但无许可证,只能抄思路重写;轩说换别家开源抄,行后再选品)、**chat-lite**(thinking 面板/缓存徽章/来源折叠直接搬;其 OpenRouter 双断点缓存要换成星露谷验证过的三断点)。
- 产出:`260721-聊天前端完整方案.md` + `260721-阶段0交互详案.md`(5 个待拍板问题在 §8)+ 可点交互示意稿 https://claude.ai/code/artifact/be509f26-bddf-454c-a546-4e6b94304aa1 (v2)。
- **设计纠偏立法**:示意稿 v1 底栏用 emoji 当图标被轩当场打回("像10年前安卓机")→v2 全换 SF Symbols 风细线 SVG+按压反馈;已写进 `~/.claude/skills/xuan-design/taste.md` 新增「图标」节:**UI 图标禁 emoji,只许细线 SVG;emoji 只活在文案里**。
- 产品痛点入档:轩天天拿 Daygram 当备忘录,嫌一天一格不够——ember 日记解除此限制(一天多条不限长度),已写进完整方案阶段 3。
- 下一棒:轩在两份方案文档里直接批注+自写交互详版愿望单+收集参考截图;批注回来消化完、5 问拍完才开工。

## 2026-07-20 · 前端方案材料夜班 + 审核台闸门漏洞修复

- 轩深夜给出前端完整愿景(手机三栏 app:聊天+群聊多模型"她当群主"/趣味功能桌面(日记/读书/音乐/纪念日历/健康)/设置含审核台),并立跨项目铁律"**方案讨论透彻前不改代码,除非明确授权**"(已进 Claude 全局记忆)。
- 双路独立调查(fable酱 WebSearch 版+Codex 详查版)结论收敛:**自建薄壳+定点抄交互,不整体 fork**。关键发现:LobeChat/Open WebUI 许可证有雷(衍生分发受限),MIT 干净的是 LibreChat/NextChat/big-AGI/ChatClaw;群聊样板=Open WebUI Channels(@点名共享时间线)+LobeHub Agent Group 规则(主持人选下一位/连续回复上限)。两报告都在 docs/产出/260720-*。
- **审核台漏洞**:轩报障"你们新写入的我审核不了"→确诊 MCP `memory_save` 直写正库绕过草稿闸(违反"AI 绕不过轩"铁律)→Codex 在 worktree 施工(fable酱写单+验收):改走 drafts.save_draft 管线,batch=`mcp-日期`,返回如实告知"待审草稿",新增 3 测试,102 全绿。分支 `feat/mcp-save-drafts`(3c104e7)按 merge 权归轩规矩**留待她定夺**,未合未部署未 push。
- 部署澄清(写进讨论底稿):前端部署在 VPS,欧洲手机直访,无需笔记本常开;mac mini 只关系"订阅 claude 当大脑"二期议题。

## 2026-07-13 · 交接体系对齐跨项目剧本

轩要求把交接流程标准化，已提炼成可复现剧本沉淀进 Claude 全局记忆（handover-playbook，从本项目 + 星露谷两边取长补短；星露谷同日已对齐——规矩文件对调成 CLAUDE.md 主 + AGENTS.md 指针、交接文档三层化，抄的正是 ember 的结构）。本项目本来就最接近标准，只微调两处：接手三步改**四步**（新增漏更校验：对比"最后更新"和 git log 日期，防上个窗口忘收工）；"过时信息直接删掉重写"措辞收紧（标明仅指仪表盘，防误读成连历史日志也删）。附实测结论：CC 自动加载 CLAUDE.md、不加载 AGENTS.md，且在根目录开会话时读到项目内文件也会自动注入项目根 CLAUDE.md。

## 2026-07-12 · 审核台三连 PR 合并部署

`feat/review-manual-add`（#12，「＋ 添加」手动补记忆，走草稿→入库管线，反悔区能撤回）→ `feat/review-palette`（#13，配色翻新，轩挑方案 A）→ `feat/review-fonts`（#14，字体升级，自托管 `backend/static/fonts` ~7.8MB，Dockerfile 加了 `COPY static`——**部署时要重新 build 镜像，不是光重启**）。三个 PR 顺序合并（GitHub 自动接力 retarget），VPS `git pull` + `docker compose up -d --build` 后审核台、fonts.css、字体文件均验证 200，全部 99 测试过。设计定稿沉淀进 `~/.claude/skills/xuan-design/`（场景 C 色板+字体）。

## 2026-07-09 · claude.ai connector 破案接通

真凶是 Cloudflare「Block AI training bots」在边缘拦了 UA `Claude-User`（出口段 `160.79.104.0/21`）的正式 MCP 请求——OAuth 换 token 阶段走 Azure IP + python-httpx 所以能过，来源不同是全案钥匙。轩在 CF Overview 把该开关改成 Do not block，app 一开就自动连上，全会话 200。完整排障六层洋葱见「技术参考 → 经验教训」。PR #10（briefing 工具）、#11（记忆库视图）已 merge + 部署 + 验证。轩确认"记忆库 V0–V6 算结束，但是要改的 bug 也很多"，进入使用+喂养期。

## 2026-07-08 · V7 冲刺启动 + 设计基建（轩：这节我按照我的意思已修改）

跟fable酱讨论计划，fable酱脑子放开了想，以及（"就算做不完，走之前你给我留好计划"7-13来看时间来得及）。目标是聊天网关 MVP（轩：？MVP到底是啥意思）。（V7 迁旧改造已取消——轩："旧系统不需要迁移，因为导入重新做过了"）。当日完成：网关后端建表+哨兵+`/chat/api/send` 全流程+测试、对话回流提取管线（借鉴 kiwi-mem 的已知记忆防重+supersedes 边建议）、kiwi-mem 精读调研（详见 `docs/产出/kiwi拆解笔记.md`）。轩原话："这个前提在于我们完全聊好了方案和计划，我才能放心的放手"——长跑工作流讨论稿见 `docs/产出/AI长跑工作流草案.md`。当晚做了一版文档体系重组（交接.md 改单文件仪表盘+日志结构，此后延续至 2026-07-13 再次重组统一到本仓库）。

## 2026-07-05 · V5 语义检索 + V6a 空间隔离

**V5（PR #8）**：hybrid 检索——关键词腿（LIKE）+ 语义腿（embedding 余弦距离）RRF 融合；指纹写入时生成、处处软失败、模型/维度全在 `.env` 不写死；生产验收：搜"怀孕"零字面重合召回"朋友要生孩子"；72 测试全过。两个反直觉取舍：FTS5 中文分词不好用，关键词腿改用 LIKE 反而对中文更好；`sqlite-vec` 的 `vec0` 虚拟表把维度写死进 DDL，改用普通表存 float32 BLOB + `vec_distance_cosine()` 换模型更灵活。

**V6a（PR #9）**：检索默认只搜 `personal` 核心层（防项目记忆稀释关系记忆），项目空间显式传，`"all"` 跨全库；`resolve_space()` 单一入口，关键词/语义/list 三处同规则；75 测试全过，部署零迁移零新变量。

**V4（2026-07-04，PR #7）**：`memory_edges` 完善（UNIQUE/CHECK/created_by/created_at）+ `start_date`/`end_date` + `interval_status` 北京时间读时现算 + 草稿 `links` 边建议随审核原子入库 + 乱序回填 + `supersedes` 同步 `superseded_by` 列；61 测试全过。设计定稿见 `docs/项目计划.md` §2.1。

## 2026-07-03 · V1 空壳 + V2 记忆链路 + V3 审核台（tag v1/v2/v3）

**V1**：仓库骨架 + FastAPI `/health` + Docker Compose + 文档。

**V2**：SQLite 三表 + 五个 MCP 工具（streamable HTTP，`MCP_PATH` 随机串当能力密钥）+ 导入解析脚本（锚点续跑）；试导入第一批 8 条（4.18–4.19，人工质检通过）已入库。Claude Code 首个真实客户端连上验收 ✅。

**V3**：OAuth 三层门禁（随机路径→口令→Bearer）+ 手机审核台 `/review` + 草稿流水线（pending→approve/reject→memories+sources）+ 反悔区（撤回重审，`superseded_by` 联动解除）；`memory_drafts` 表兼任导入审计；逐月开采（Batch A–J，4.20–6.1）70 条草稿已推审核台。

## 2026-06 · V0 调研

五个开源记忆系统对比（kiwi-mem / Paramecium / AZOTH_mem 等）、旧系统 `/root/memory`（Haven Fork）试用两周、痛点清单、两轮施工计划定稿。旧系统证明了 VPS + Docker Compose + Cloudflare Tunnel + MCP 这条链路可行，但代码耦合重，小改动牵连大逻辑——ember 因此决定从干净仓库开始，每版只做一条真实可跑的小链路。本地归档，不入库。

---

# 技术参考

> 按需查阅，不用每次开工都读。改代码/部署/排障时对应章节找答案。

## 项目概览

- 项目：ember —— 个人 AI 长期记忆中枢（关系连续性为核心 + 项目协作记忆为附加）
- 仓库：https://github.com/cloudxuan1/ember （本仓库，公开，代码+文档）
- 域名：`ember.cloudxuan1.com`
- 服务：FastAPI，跑在 VPS Docker Compose，公网走 Cloudflare Tunnel

**名称对照**：`ember` = 本项目（也是 VPS 主机名）｜`memory` = 旧系统（Haven Fork，跑在 `/root/memory`，域名 `memory.cloudxuan1.com`，只作参考样板，不拆不修不迁）｜`chat-lite` = 独立的自建聊天前端项目，与本仓库无关。

## 架构

```text
长期链路（V2 起）：
客户端（Claude.ai / ChatGPT / Claude Code / Codex / 自写）
  → MCP / REST
  → FastAPI（Cloudflare Tunnel 转发，宿主机 127.0.0.1:8300 只作本机调试）
  → SQLite ember.db（data/ 卷，向量走 sqlite-vec 思路的普通表）
```

## 部署和运维

- VPS：主机名 ember，2G 内存 + 2G swap，与旧系统 memory 平行跑（各自 Tunnel、各自端口，互不影响）。IP/账号等连接方式见本地《VPS 维护文档》（不入库）。
- 更新部署：见「仪表盘 → 常用命令」。PR #15 合并并首次安装后，cron 每 5 分钟只做一次轻量 `git fetch`；`flock` 防止两轮重叠。无提交静默退出，纯文档只快进；`backend/**` 或 `docker-compose.yml` 变化才重建。事件写入 `data/autodeploy.log`，超过 1MB 自动轮转并保留 3 份。
- 自动回滚：每次健康部署记录在 `data/autodeploy.good`；新版本 build 失败或 120 秒内未恢复健康时，程序回到该提交并重建，坏提交写入 `data/autodeploy.skip`，直到 main 出现更新或人工 `FORCE=1` 才重试。数据库不会自动回退。
- 备份：运行代码部署前，`scripts/sqlite_backup.py` 使用 SQLite Backup API 生成一致性快照并校验，只保留最近 5 份 `auto-*`；手工备份和现有日/周备份不由自动部署清理。

## 数据库

SQLite（`data/ember.db`，Docker 卷挂载）。五张表：`memories` / `memory_edges` / `memory_sources` / `memory_drafts` / `memory_embeddings`（每行带 model 列，记忆删除时级联删）。完整表结构和设计理由见 `docs/项目计划.md` §2。

## MCP

- 端点：`https://ember.cloudxuan1.com/<MCP_PATH>/`（streamable HTTP，有状态 SSE 模式——claude.ai 只认这种）
- 鉴权三层（`backend/app/oauth.py`）：随机路径 → 授权页口令 → 每个请求验 Bearer token。密钥位置见「仪表盘 → 常用命令」，不写明文。
- 客户端接入：
  - **Claude Code**（✅ 已验收）：`claude mcp add --scope user --transport http ember "<完整URL>/" --header "Authorization: Bearer <token>"`
  - **claude.ai 网页 connector**（✅ 已接通，2026-07-09）：走标准 OAuth 流程；接通过程见「历史日志」及本节「经验教训」
  - **ChatGPT**：理论兼容（标准 OAuth 2.1），未实测
- 五个工具：`memory_search` / `memory_recall` / `memory_save` / `memory_list` / `memory_status`，实现在 `backend/app/mcp_server.py`；第六个工具 `memory_briefing(topic?)` 是主动浮现入口。**工具的 docstring 就是发给客户端模型的说明书**，改行为必须同步改说明。
- **space 语义**：search/list 不传 space = 只搜 personal 核心层；项目话题显式传（ember/vps/...）；跨全库传 `"all"`。入口统一在 `memories.resolve_space()`。
- `换 embedding 模型` 操作步骤见「仪表盘 → 常用命令」。

## 审核台

- 地址：`https://ember.cloudxuan1.com/review`（手机浏览器直接用）；登录态 30 天。
- 草稿入口：`POST /review/api/drafts`，与 MCP 的 token 刻意分开（持 MCP token 的一方不该开得了审核台）；单条或批量 `{"drafts": [...]}`，**整批原子**。
- 流程：提取推草稿（pending，带 batch 批次名）→ 手机上 通过/改/删 → 通过才写入 memories + sources。
- **反悔区**：右上"↩ 已审核"，通过/删过的都能撤回重审；通过的撤回会连生成的记忆一起删；被 `superseded_by` 引用的拒绝撤回会 409。
- 实现：`backend/app/drafts.py`（存取）+ `backend/app/review.py`（页面+API）。

## 数据与隐私

- 本仓库只放代码和脱敏文档，密钥/端口/账号具体值一律不写，只写"在哪能找到"。
- 历史日志原文放**单独的私有仓库**（获得版本历史+异地备份+多机同步三件事），`ember.db` 只存在 VPS 卷和备份里，永不进本仓库。

## 绝对不能动

- 旧系统 `/root/memory` 的一切：`buckets`、`.env`、`config.yaml`、docker-compose、旧 Tunnel 路由、`memory.cloudxuan1.com` 指向。不拆、不修、不迁、不覆盖。
- `.env` / `*.db` / 日志原文 / 任何个人记忆数据不进本仓库。
- 禁止 `docker compose down -v`（会删卷）。
- 未经确认不直接推 main；应用端口不改成 `0.0.0.0` 裸露公网；AI 不读取、不提交真实密钥。

## 经验教训

- FastMCP 默认开 DNS-rebinding 防护且 Host 白名单只有本机，放在反代/Tunnel 后面公网请求会被 421 拒——要显式传 `TransportSecuritySettings` 把公网域名加进 `allowed_hosts`。
- Claude MCP client 不跟 307 redirect，遇到无尾斜杠路径会误判成 sign-in 失败。根因是 Starlette Router 默认 `redirect_slashes=True` 触发 307。修法：包一层 `_MCPPathFix` ASGI 中间件原地改写路径。
- **claude.ai 网页 connector 排障六层洋葱**（2026-07-03 起，2026-07-09 剥完接通）：① 其客户端不跟 307 → 中间件改写路径；② 只认有状态 SSE，无状态 JSON 判失败；③ 现行 connect 流程强制走 OAuth；④ `token_type` 必须大写 `"Bearer"`（其后端 model 是 `Literal["Bearer"]`，小写静默炸）；⑤ token 交换后一度看似"静默中止"；⑥ **真凶：Cloudflare「Block AI training bots」**——claude.ai 的 OAuth 请求（python-httpx UA，Azure IP）与换 token 后的正式 MCP 请求（UA `Claude-User`，出口段 `160.79.104.0/21`）来源不同，后者被 CF 边缘静默丢弃。关掉该开关（Overview 页下拉框，与 Bot Fight Mode 是两个开关）即通。同款案例：claude-ai-mcp #438、#125、#519。排障方法论：拿旧系统当对照组逐项消差异；给请求头/请求体加透视日志（脱敏）拿实锤，别靠猜；"排除一个功能 ≠ 排除整个层"——查过 Bot Fight Mode 没开就以为 CF 无责，漏了独立的另一个开关。
- V5 换 embedding 模型是常规操作，流程见「仪表盘 → 常用命令」，中间态安全降级不会炸库。
- 模块级懒状态要给"独立进程第一个入口"留路：`_vec_status` 只在 `get_conn()` 里点亮，`python -m app.embeddings` 单独跑时没人开过连接就来问会误报——教训通用：任何"初始化时顺手设置"的全局状态，都要想想不走常规初始化路径的入口。
- docker-compose 的 `environment` 是显式白名单：加新环境变量必须同步加透传行，否则容器里静默拿不到（V3/V5 两次踩同一坑）。。
- CC auto 模式分类器会拦「AI 自改权限/配置」「生产库裸 SQL 写」「无条件 DELETE」这类操作——遇到停下来让轩决定，设计时尽量走应用层 API 绕开。
- 导入功能"暂停不能继续"是旧系统最痛的坑——ember 的导入管线为此专门设计断点续跑和人工质检。

## 调研和参考

- kiwi-mem: https://github.com/LucieEveille/kiwi-mem —— Dream 三层、软化机制、日历层级压缩，精读笔记见 `docs/产出/kiwi拆解笔记.md`
- Paramecium: https://github.com/Shitsuten/paramecium —— 原文证据 + 目录先行按需 recall + 分层缓存
- AZOTH_mem: https://github.com/TAra93-1/AZOTH_mem —— 记忆/原文/链接三分观、状态模型
- 调研原始笔记与决策记录：本地归档，不入库

## 杂项笔记

- 域名规划：`memory.*`（旧系统，暂不动）/ `ember.*`（本项目）/ 远期 `mcp.*`、`api.*`
- ember 稳定后再议是否把主域名切过来，旧系统留观察期
